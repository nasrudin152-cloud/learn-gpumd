# nep.in for Au-O-H 体系训练

# 基础设置
version       4           # 推荐使用 NEP4 版本，对多组分体系精度更高 
type          3 Cu C O    # 必须设置：元素种类数量及符号（区分大小写） 

# 描述符参数
# 对于含 H 体系，建议先从默认值开始测试，若收敛困难可尝试微调 
cutoff        6.0 3.5         # 径向截断 8 Å，角度截断 4 Å 
n_max         4 4         # 径向和角度描述符基数，默认 4 4 通常兼顾速度与精度 
basis_size    8 8         # 径向和角度基函数数量，默认 8 8 通常足够 
l_max         4 2 0       # 角度展开阶数，NEP4 支持三体到五体项 

# 神经网络规
neuron        30          # 隐藏层神经元数量，30 是平衡效率与精度的通用选择 

# 损失函数权重
lambda_e      1.0         # 能量误差权重，默认为 1.0 
lambda_f      1.0         # 受力误差权重，默认为 1.0 
# 若 train.xyz 中包含位力数据，设为 0.1；若无位力数据，请设为 0.0 
lambda_v      0.1         

# 训练控制
batch         1000        # 每代随机抽取的结构数，建议不小于 1000 
population    50          # 种群大小
generation    200000      # 训练总代数，对于三元体系，建议至少 10^5 步以确保收敛 
